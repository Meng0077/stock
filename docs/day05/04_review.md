# 4. 人工复核与报告

**TODO D05-Core-2：由你完成最终事实判断。** 输入是 `docs/day05/report.md` 指向的结果文件；每项有 `run_id`、`final_output`、`input_snapshot.evidence` 和工具成功事件的 `fixture_result`。输出是在报告表格中为每项写 `pass/fail` 和具体理由。不要仅因自动检查 `pass` 就写人工 `pass`。

逐条核对：引用是否真的支持句子，数字/单位/同比环比/报告期是否混淆，缺资料时是否承认，矛盾是否指出，事实与推断是否分开，是否把本地 fixture 说成实时行情。D05-09 的 E99 是归属错误；即便 ID 合法，也可能引用了不支持该句话的资料。行为预期失败被正确阻止，只能算自动规则通过，不能算正常分析成功。

报告分开统计自动符合率、人工通过数、blocked 数和真实请求成功数。未知用量写 `null`，不要虚构稳定性或收益提升。
