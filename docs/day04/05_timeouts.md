# 5. 单工具超时、任务总时限与预算

先在 `backend/examples/timeout_cancel.py` 离线练习，再与 Task 6 的异步客户端一起整合到 `backend/examples/structured_agent.py`。Task 5 的离线实现与验收已完成；真实服务请求仍待 API 限额恢复后验证。

## 按顺序完成的 TODO

- [x] **TODO 5.1，离线模拟工具**：完成快、慢两个 `async` 工具，用 `await asyncio.sleep()` 模拟可取消的 I/O，并能记录工具是否真正执行。位置：`backend/examples/timeout_cancel.py`。
- [x] **TODO 5.2，工具局部超时**：用 `asyncio.timeout()` 只包住一次工具等待。快工具正常返回；慢工具超时后生成 `tool_timeout` 结果供上层处理。
- [x] **TODO 5.3，任务总时限（离线）**：在一次任务入口建立外层 `asyncio.timeout()`，让多个异步步骤共用同一个时限，不在每轮重新计时；真实模型和格式修复步骤留待 TODO 5.6 整合。
- [x] **TODO 5.4，区分超时归属（离线）**：工具自己的超时可回传 `tool_timeout`；外层总超时直接结束整次任务。不能把总时限触发的取消当成工具失败后继续执行。
- [x] **TODO 5.5，预算与用量**：保留 `MAX_MODEL_ROUNDS=3`、`MAX_TOOL_CALLS=4`；修复和最终生成仍计入模型轮数。每次模型请求有输出 token 上限，响应的实际用量写入运行事件。单次输出上限不是整次任务的费用硬上限。
- [x] **TODO 5.6，异步 Agent 整合（离线）**：通过 `httpx.AsyncClient` 发送真正可等待的模型请求，在任务入口设置一次总时限，覆盖模型、工具与格式修复。真实 API 请求仍待限额恢复后验证。
- [x] **TODO 5.7，离线验收（由我实现测试）**：已检查快工具成功、慢工具局部超时、多个步骤累计触发总超时；总超时后不再发起下一轮模型请求或执行剩余工具，并验证预算及用量记录。

## 注意

- 在 async def 内调用阻塞 SDK 或 time.sleep，仍会阻塞事件循环，超时可能不能及时执行。
- 当前报价和公司资料 handler 仍是同步读取本地 fixture；真实网络工具接入时必须使用可 `await` 的 I/O，再验收 5 秒工具超时。
- 本练习仅用可取消的异步等待验证；不要声称线程包装可强制终止底层同步调用。
- asyncio 超时依赖协作式取消和清理，不是强制杀进程的硬截止。
- 一次远端调用本地超时后，服务端仍可能完成并产生费用。

## 验收

快工具成功；慢工具触发局部超时；多个步骤累计超时触发总终态。
记录超时后后续模型/工具的调用次数为 0，不仅打印“超时”就算通过。
