# Day3 验收记录（2026-09-13）

## 离线模拟

- 来源：`offline_simulation`，未请求模型服务。
- `verify_manual_agent.py`：7 项通过，0 项失败。
- 工具参数和白名单测试：17 项通过。
- [事件示例](offline_simulation_events.json)：一轮请求两个工具，按各自 ID 回传后得到模拟最终回答。

## 真实模型：教学模拟报价

- 来源：`real_api`；本次使用配置文件中的模型，未保存 API Key。
- 问题：仅查询 NVDA 的教学模拟报价，要求调用 `get_quote` 并说明不是实时行情。
- [脱敏事件](real_api_quote_events.json)：2 轮模型请求、1 次 `get_quote` 成功，工具请求与结果的 ID 一致，终态为 `success`。
- 模型请求耗时分别为 865.901 ms、1530.88 ms；Token 用量分别为 405、584。
- 人工核对：最终回答写出 **100.0 USD**、**2026-09-11 09:00（北京时间）**，并明确说明这是本地教学模拟数据，不是实时行情；这些值与工具返回的 fixture 一致。
- 结论：报价工具的真实往返通过。

## 真实模型：公司资料

- 来源：`real_api`；问题要求调用 `get_company_profile` 查询 NVDA 的公司名称和业务简介。
- 用户随后手动运行成功。[成功事件](real_api_profile_events.json)显示 2 轮模型请求、1 次 `get_company_profile` 成功；工具请求与结果的 ID 一致，返回内容与公司资料 fixture 完全一致，终态为 `success`。
- 模型请求耗时分别为 605.636 ms、1363.047 ms；Token 用量分别为 408、598。
- 人工核对：用户提供的最终回答写出 **NVIDIA（英伟达）**、GPU 及相关计算平台用于图形处理和人工智能计算，标明资料来自本地教学模拟数据，并给出 **2026-09-11 09:00（北京时间）**。这些内容与公司资料 fixture 一致；最终一轮的 598 Token 也与事件记录一致。
- 结论：公司资料工具的真实往返和最终回答均通过。
- 另有请求返回 HTTP 429、业务码 1305；保留[失败事件示例](real_api_profile_overload_events.json)供排查。这些失败尝试不改变上述成功运行的验收结果。

所有保存的事件文件都标明来源；已核对不含配置的 API Key。事件中未保存完整请求、原始异常或模型内部推理。
